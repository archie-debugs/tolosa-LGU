"""Legacy routing migration placeholder retained intentionally empty.

The current documents model no longer persists routing_history, so the
obsolete backfill script has been removed from the active runtime path.
"""

if __name__ == "__main__":
    print("Routing history migration is not applicable in the current document model.")
